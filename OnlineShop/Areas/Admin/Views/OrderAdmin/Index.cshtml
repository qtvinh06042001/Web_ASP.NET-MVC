@model OnlineShop.Areas.Order_OrderDetail

@{
    ViewBag.Title = "Index";
}

<h2>Quản lý đơn hàng</h2>


<table class="table">
    <tr>
        <th>
            <p>Name</p>
        </th>
        <th>
            <p>UserId</p>
        </th>
        <th>
            <p>CreateOnUtc</p>
        </th>
        <th>
            <p>Phone</p>
        </th>
        <th>
            <p>Address</p>
        </th>
        <th></th>
    </tr>

@foreach (var item in Model.lstOrder) {
    <tr>
        <td>
            @item.Name
        </td>
        <td>
            @item.UserId
        </td>
        <td>
            @item.CreateOnUtc
        </td>
        <td>
            @item.Phone
        </td>
        <td>
            @item.Adress
        </td>
        <td>
            @*@Html.ActionLink("Xem", "Details", new { id=item.Id })*@ <a href="~/Admin/OrderAdmin/Details/@item.Id"><img style="width:20px" src="~/Content/images/details.png" /></a> |
            @*@Html.ActionLink("Xóa", "Delete", new { id=item.Id })*@ <a href="~/Admin/OrderAdmin/Delete/@item.Id"><img style="width:20px" src="~/Content/images/delete.png" /></a>
        </td>
    </tr>
}

</table>
